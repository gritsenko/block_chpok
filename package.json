{
  "name": "block-chpok",
  "version": "1.0.0",
  "private": true,
  "description": "Static browser game with source files in src and publish output in dist.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.mjs --target=web",
    "build:yandex": "node scripts/build.mjs --target=yandex",
    "build:gameads": "node scripts/build.mjs --target=gameads",
    "build:jam": "node scripts/build.mjs --target=jam",
    "build:all": "npm run build:yandex && npm run build:gameads && npm run build:jam",
    "clean": "node scripts/build.mjs --clean"
  }
}